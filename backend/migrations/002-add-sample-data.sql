-- Optional: Add more sample data if needed
-- This migration is optional and can be run separately

-- Add more template translations if needed
-- Add more system users if needed
-- Add sample requests for testing

DO $$ 
BEGIN
    RAISE NOTICE 'Sample data migration completed (if any data was added)';
END $$;